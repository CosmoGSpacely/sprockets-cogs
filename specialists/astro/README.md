# Astro

Astro owns the vault-facing surface: rendered notes, manual carry affordances,
and human-readable inspection views.

Astro is not merely a ledger writer. The vault is also where a user can inspect,
close, drop, and carry Cogs manually, so Astro is the boundary that keeps human
work surfaces explicit while shared validation stays in substrate.

