#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
exec .venv/bin/python -c 'from specialists.orbit.pilot3 import telegram_main; telegram_main()' --watch --wait-seconds 30 --timeout 20 "$@"
