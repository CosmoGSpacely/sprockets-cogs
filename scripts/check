#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

.venv/bin/python -m unittest discover -v
.venv/bin/python -m specialists.uniblab.smoke_test
scripts/fallback-eval
scripts/review --count
